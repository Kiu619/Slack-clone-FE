import { Channel } from '@/lib/types'
import React from 'react'

export default function SettingsTab({ currentChannelData, onOpenChange }: { currentChannelData: Channel, onOpenChange: (open: boolean) => void }) {
  return (
    <div>settings-tab</div>
  )
}
